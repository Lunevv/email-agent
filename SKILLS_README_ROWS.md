# Строки для `skills/README.md` основного репозитория

Вставить в таблицу «Список»:

```markdown
| [`rusender-agent`](rusender-agent/) | 1.0.0 | Память по нескольким проектам: контекст продукта, опознание проекта по задаче, история кампаний | — |
| [`rusender-preflight`](rusender-preflight/) | 1.0.0 | Проверка черновика перед отправкой: ссылки, UTM, переменные, отписка, пересечения аудиторий | — |
| [`rusender-results`](rusender-results/) | 1.0.0 | Статистика рассылки и сравнение с сопоставимой серией того же проекта | — |
```

Ссылки на инструкции проставить, когда появятся страницы на сайте.

Строку для раздела «Ограничения» в `skills/README.md`:

```markdown
- `rusender-preflight` требует Node 18+ и доступа в сеть для обхода ссылок; без них проверка ссылок
  помечается как невыполненная.
- `rusender-agent` и `rusender-results` хранят память в файлах рабочей папки, поэтому в claude.ai
  работают без сохранения контекста между диалогами.
```
